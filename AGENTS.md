# AGENTS 编码规范

这是一个 fork 项目.

## flutter

windows 中的 flutter 和 dart 使用 scoop 安装, 因此可能环境变量中不存在这两个可执行文件,
不要手动执行 dart 和 flutter 命令, 因为可能会导致过长的等待时间, 请你留给用户手动测试.

## merge

在 merge 上游仓库的时候, 尝试解决项目中的冲突,
你可以根据 README.md 开头中的内容判断我方的更改,
保留我方修改, 除非上游仓库已经完全实现了我方功能, 保留最优雅完全的实现, 如果遇到你不能做出抉择的, 请你告诉我.
如果上游项目完成了 README 开头中提到的功能, 那么使用 `- [x]` 勾选这个项.
解决完成之后, 说明:

- 自动合并 (没有产生冲突的部分) 了什么内容, 是否会产生非预期的作用.
- 上游做出了什么更改, 具体到函数和同级别.
- 我方更改是否和上游更改有冲突, 有什么冲突.
- 你解决冲突的思路, 是否完美解决, 是否可能有副作用.
- 你未能解决的冲突.

merge 的时候不要使用 git 的任何有副作用的操作, 留给我审核处理.

为了方便 merge, 请你每次进行改动的时候都是最小化的改动,
除非我的要求, 否则你不要自作主张进行不必要的源代码改动,
并且
- 不要对代码进行格式化;
- 不要删除注释;

## widgets

添加控件的时候需要参考项目其他控件的风格, 防止割裂感的产生.

## readme

新添加的功能描述在 README 中放在列表的最后.
在进行开发的时候, 同步在 README 的开头写上和上游项目的功能区别.
